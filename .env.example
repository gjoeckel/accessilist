# AccessiList Environment Configuration
# Copy this file to .env and configure for your environment
# DO NOT commit .env to git - it contains environment-specific settings

# =============================================================================
# ENVIRONMENT SELECTION
# =============================================================================
# Options: local, production, staging
# This is the SINGLE SOURCE OF TRUTH for environment detection
APP_ENV=local

# =============================================================================
# BASE PATH CONFIGURATION
# =============================================================================
# Base path prepended to all URLs (assets, pages, APIs)
# Local development typically uses empty string (root)
# Production/Staging use subdirectory paths

# Local Development (localhost, 127.0.0.1, port 8000)
BASE_PATH_LOCAL=

# Production Server (webaim.org)
BASE_PATH_PRODUCTION=/training/online/accessilist

# Staging Server (if applicable)
BASE_PATH_STAGING=/staging/accessilist

# =============================================================================
# API CONFIGURATION
# =============================================================================
# File extension for API endpoints
# Local: .php (direct file access)
# Production: empty (handled by .htaccess rewrite rules)

API_EXT_LOCAL=.php
API_EXT_PRODUCTION=
API_EXT_STAGING=

# =============================================================================
# OPTIONAL: DEBUG MODE
# =============================================================================
# Enable detailed error reporting and logging
# Should be false in production

DEBUG_LOCAL=true
DEBUG_PRODUCTION=false
DEBUG_STAGING=true

# =============================================================================
# SETUP INSTRUCTIONS
# =============================================================================
# 
# LOCAL DEVELOPMENT:
# 1. Copy this file: cp .env.example .env
# 2. Keep APP_ENV=local
# 3. Start server: php -S localhost:8000
# 
# PRODUCTION DEPLOYMENT:
# 1. Copy this file to server: cp .env.example .env
# 2. Change APP_ENV=production
# 3. Ensure .env is NOT in git (check .gitignore)
# 
# SECURITY NOTE:
# - Never commit .env to git
# - Use server environment variables when possible
# - Keep sensitive data (passwords, keys) in server env vars
# 
# =============================================================================
